doctype html
html(lang="en")
    head
        title Update contact info
        link(rel="stylesheet", href="/css/style.css")
    body
        .form-wrapper
            p User: #{userEmail}
            p Favorite Book: #{favoriteBook}
            form(method="POST")
                label(for="newVal") New favorite book
                input(type="text" name="newVal" required)
                input(type="submit" value="Submit")